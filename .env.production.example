# Production Environment Configuration

# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (use connection pooling in production)
DATABASE_URL=postgresql://username:password@prod-db-host:5432/marketviz_production?connection_limit=10&pool_timeout=30

# JWT Configuration (MUST use a strong secret in production!)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS

# Alpha Vantage API (get your production key from https://www.alphavantage.co/support/#api-key)
ALPHA_VANTAGE_API_KEY=your_production_api_key_here

# CORS Configuration (restrict to your production domains)
CORS_ORIGIN=https://your-production-domain.com,https://www.your-production-domain.com

# Rate Limiting (stricter for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Optional: External Services
# SENTRY_DSN=your_sentry_dsn_here
# NEW_RELIC_LICENSE_KEY=your_newrelic_key_here
